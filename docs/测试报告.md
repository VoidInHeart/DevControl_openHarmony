# DevControl V1.2 测试报告

测试日期：2026-07-19
主机环境：Windows，Python 3.13.9（`D:/Anaconda3/python.exe`），HarmonyOS SDK 6.1.1 / API 24

## 自动化结果

| 测试项 | 命令 | 结果 |
| --- | --- | --- |
| 网关单元与协议测试 | `python -m pytest -q` | 30 passed |
| `dev_cly` 合并回归 | 13 台默认快照、8 设备离家、4 设备回家、空调四档风速 | 30 passed；模块化驱动架构保留 |
| 多传输合约 | HTTPS 命令、WSS、SSE、MQTT 5 TLS 桥接替身 | 通过；统一安全处理链 |
| 设备驱动扩展与智能窗帘 | 驱动注册、Schema、加密命令、幂等与状态推进测试 | 通过 |
| 新设备接入演练 | `fan-bedroom-01` 快照、通用控件、加密命令与非法风速 | 通过；APP 无新增类型分支 |
| HTTPS/WSS 冒烟 | `python scripts/e2e_smoke.py` | 通过；TLS 最低 1.2 |
| 安全负向 | `python scripts/security_negative_test.py` | 7 项通过 |
| 1000 条性能测试 | `python scripts/performance_test.py --count 1000` | 通过 |
| 30 秒稳定性冒烟 | `python scripts/run_e2e_suite.py --performance-count 100 --stability-seconds 30` | 通过 |
| 敏感信息扫描 | `scripts/security_scan.ps1` | 通过 |
| Debug HAP 构建 | `scripts/build_app.ps1 -BuildMode debug` | 2026-07-19 BUILD SUCCESSFUL，ArkTS 编译错误 0；存在既有异常处理和弃用 API 警告 |
| Release HAP 构建与签名验证 | `scripts/build_app.ps1 -BuildMode release`、SDK `verify-app` | 本轮未复测；需要被忽略的本机签名配置 |
| 一次性配对码与凭据过期 | pytest、协议 Schema、HTTPS/WSS | 配对成功/到期轮换；默认 24 小时凭据到期后 REST 与已连接 WSS 均拒绝 |
| HUKS 保存与恢复 | API 24 模拟器、`GatewaySessionStore.ets`、APP 强制停止后重启 | 配对保存、重启恢复和四设备快照加载通过；`authTag` 以 `HUKS_TAG_AE_TAG` 独立传入 |

安全负向覆盖：

- 系统信任库不接受演示私有 CA；
- 合法 CA 可以完成 HTTPS/WSS；
- 密文篡改、AAD 篡改、超过 30 秒时间戳、复用 nonce 被拒绝；
- 重复 messageId 返回缓存结果且不重复执行；
- 无效 Bearer credential 不能建立有效事件会话。
- 凭据在 `expiresAt` 边界立即失效；已建立的 WSS 以 4401 关闭，后续 REST 返回 `AUTH_FAILED`。

## APP 架构与导航回归（2026-07-19）

| 检查项 | 方法 | 结果 | 说明 |
|---|---|---|---|
| Debug HAP 构建 | `scripts/build_app.ps1 -BuildMode debug` | 通过 | 底部“家/智能/设置”、全屋/房间分段、规则持久化、场景编辑、门锁 SVG 和空调风速合并后构建成功 |
| ArkTS Local Test | `hvigorw test -p product=default -p module=entry@default -p buildMode=debug --no-daemon` | 9 / 9 通过 | 设备模型 5 项、设备注册表 4 项；失败和错误均为 0 |
| ArkTS 覆盖率 | Hvigor Local Test 报告 | 总行覆盖 65.14%（441/677）；`DeviceRegistry` 行覆盖 77.90%，函数 100% | UI 与 Preferences 依赖代码仍需 on-device 回归 |
| 智能规则持久化 ohosTest | `SmartRuleStore.test.ets` | 测试源码已加入 | 需要签名测试包和模拟器/真机执行，当前不能标记为通过 |

本轮合并后的窗帘和风扇均为默认快照中的可控通用能力设备；网关 pytest 已覆盖快照、Schema、加密命令、非法参数及状态推进，HAP 构建通过。灯光滑块、房间复位、智能页规则/场景编辑、空调风速和门锁远程解锁确认仍需在目标设备或模拟器上执行最终交互验收。

## 性能结果

| 指标 | 结果 | 门槛 |
| --- | --- | --- |
| 样本数 | 1000 | 1000 |
| 成功率 | 100% | ≥99.5% |
| 命令平均延迟 | 3.273 ms | 记录项 |
| 命令 P95 | 4.188 ms | ≤500 ms |
| 命令最大延迟 | 11.641 ms | 记录项 |
| 状态事件平均延迟 | 3.543 ms | 记录项 |
| 状态事件 P95 | 4.538 ms | ≤1000 ms |
| 状态事件最大延迟 | 11.985 ms | 记录项 |

该结果为同机回环 TLS/WSS 测试，不替代真实局域网和真机延迟。

## 稳定性冒烟

30 秒测试收到 3 次心跳、16 次状态事件并完成 3 次全量快照；网关 RSS 从 58.500 MB 增至 58.801 MB，最大增长 0.301 MB。此结果只证明短时冒烟，正式验收仍需运行：

```powershell
.\scripts\test_stability.ps1 -DurationSeconds 1800
```

## 构建与交付

- ArkTS、Native C++、资源编译和 HAP 打包通过。
- 仓库默认不含签名配置，因此日常脚本生成无签名 HAP。
- 当前工作区未保留签名 HAP；需要安装到真机或执行 ohosTest 时，应使用被忽略的本地签名配置重新构建并用 SDK `verify-app` 复核包签名、摘要和 Native 代码签名。
- 开发调试 profile 生成的签名包只用于本地安装和竞赛演示，不作为商店发布签名。
- 私钥、数据库、报告和本地签名配置均被忽略。

## 尚缺设备证据

API 24 模拟器此前已完成 APP 启动、HTTPS 健康检查、网关配对、HUKS 会话保存、APP 强制停止后重启恢复和四设备快照加载。该证据早于本次 13 设备与智能页合并，不能替代当前版本的完整页面验收。代码已修复 `isKeyItemExist()` 的首次建密钥兼容行为，并防止 `abortSession()` 覆盖原始异常；认证标签篡改拒绝的 ohosTest 已加入并完成编译，但命令行设备执行仍需本机签名配置。下列证据仍待补充：

- API 24 模拟器 13 设备快照、家/智能/设置导航、规则与场景持久化、空调风速、通用窗帘/风扇、断线回配对页和地址记忆；
- HarmonyOS 6.1.1 真机 HUKS 保存、恢复、覆盖和篡改失败；
- Native AES-GCM 与 Python 网关真实互通；
- APP 前后台、网络切换、网关重启后的回配对页与重新配对；
- 错误证书在 NetworkKit 上的实际拒绝；
- 真机连续 30 分钟稳定性及资源记录。
